{{- $tenantName := include "tenant.name" . }}
{{- $tenantNs := include "tenant.tenantNamespace" . }}
{{- $tenantAppProjectName := include "tenant.tenantAppProjectName" . }}
---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: {{ $tenantAppProjectName }}
  namespace: {{ .Values.argoNamespace }}
  labels:
    {{- include "tenant.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: {{ $tenantAppProjectName }} project
  sourceNamespaces:
    - {{ $tenantNs }}
  sourceRepos:
    {{- include "tenant.applicationSourceRepos" . | nindent 4 }}
  destinations:
    - namespace: {{ $tenantNs }}
      server: https://kubernetes.default.svc
  namespaceResourceWhitelist:
    - group: argoproj.io
      kind: Application
  roles:
    - name: viewer
      description: "Read-only access to all {{ $tenantName }} project Applications"
      groups:
      {{- range (index .Values.roleGroups "viewer") | default (list (printf "%s-viewer" $tenantName)) }}
        - {{ . | quote }}
      {{- end }}
      policies:
        - "p, proj:{{ $tenantAppProjectName }}:viewer, applications, get, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
    - name: developer
      description: "Self-service sync and actions on {{ $tenantName }} project Applications"
      groups:
      {{- range (index .Values.roleGroups "developer") | default (list (printf "%s-developer" $tenantName)) }}
        - {{ . | quote }}
      {{- end }}
      policies:
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, get, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, update, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, update/*, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, delete, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, delete/*, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, sync, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
        - "p, proj:{{ $tenantAppProjectName }}:developer, applications, action/*, {{ $tenantAppProjectName }}/{{ $tenantNs }}/*, allow"
